import { notFound } from "next/navigation"; import { prisma } from "@/lib/db"; import EbookForm from "../EbookForm"; import { saveEbook } from "../actions"; export default async function Edit({params}:{params:Promise<{id:string}>}){const ebook=await prisma.infoEbook.findUnique({where:{id:(await params).id}});if(!ebook)notFound();return <div><h1 className="mb-6 text-2xl font-black">Modifier l’ebook</h1><EbookForm ebook={ebook} action={saveEbook.bind(null,ebook.id)}/></div>}
