"use client"; import { useState } from "react";
export default function PaymentForm({slug}:{slug:string}) { const [pending,setPending]=useState(false),[error,setError]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/touba-infos/ebooks/payment",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(f))});const d=await r.json();if(!r.ok){setError(d.error||"Paiement indisponible.");setPending(false);return;}window.location.assign(d.checkoutUrl);} return <form onSubmit={submit} className="mt-6 grid gap-3 rounded-2xl border border-neutral-200 bg-white p-5"><h2 className="font-black text-neutral-900">Acheter cet ebook</h2><input type="hidden" name="slug" value={slug}/><label className="text-sm font-semibold">Nom complet<input required name="name" className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm font-semibold">Email<input required type="email" name="email" className="mt-1 w-full rounded-lg border p-2.5"/></label><label className="text-sm font-semibold">Téléphone sénégalais<input required name="phone" placeholder="77 123 45 67" pattern="(?:\\+221|221)?7[05678][0-9]{7}" className="mt-1 w-full rounded-lg border p-2.5"/></label><fieldset><legend className="text-sm font-semibold">Paiement</legend><div className="mt-2 flex gap-4"><label><input required type="radio" name="method" value="WAVE"/> Wave Sénégal</label><label><input required type="radio" name="method" value="ORANGE_MONEY"/> Orange Money Sénégal</label></div></fieldset>{error&&<p role="alert" className="text-sm text-red-600">{error}</p>}<button disabled={pending} className="rounded-xl bg-green-600 px-4 py-3 font-bold text-white disabled:opacity-60">{pending?"Redirection…":"Payer avec PayDunya"}</button></form> }
